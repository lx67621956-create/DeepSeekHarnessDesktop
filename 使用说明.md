# DeepSeek Harness 桌面版 —— 使用与维护说明

> 官方 DeepSeek Harness（dsh）的 Windows 桌面壳。壳层不碰官方代码，官方怎么更新都不影响壳层；角标/主题/托盘全是壳层独立层。

## 一、两个产物怎么用

| 文件 | 用途 |
|---|---|
| `dist\DeepSeek Harness 桌面版 Setup 0.1.0.exe` | 安装版。双击安装，**可自选安装盘符**（D 盘、E 盘随便选），桌面+开始菜单建快捷方式 |
| `dist\DeepSeek Harness 桌面版 0.1.0.exe` | 便携版。单个文件，**直接放移动硬盘/U 盘**，双击即跑，数据自动落在 exe 旁边的 `data\` 文件夹，拔盘带走 |

- 安装版默认数据在 `%APPDATA%\dsh-desktop\`；想让它跟盘走 → 设置页开"便携数据模式"（自动重启生效）。
- 首次启动约需 1~2 分钟（dsh 首次初始化 profile），之后启动几秒。窗口会显示等待进度，不是卡死。

## 二、功能清单

1. **内嵌官方 Web UI**：`dsh web` 以 node 侧车进程运行（固定版 0.1.0-rc.6），端口自动探测 3080~3090。
2. **4 套护眼主题**（设置页 → 外观）：深海蓝（默认）/ 深夜黑 / 护眼墨绿 / 暖纸色（浅色）。作用于壳层、角标、网页选区/滚动条高亮。
3. **右下角角标**：默认"作者 B站频道"→ https://space.bilibili.com/34234499 。设置页可改文字和链接，**留空链接即隐藏**。
4. **托盘常驻**：关闭窗口=最小化到托盘；托盘菜单含显示/设置/数据目录/B站/官方仓库/重启 Harness/退出。
5. **全局快捷键**：`Ctrl+Shift+Space` 呼出/隐藏窗口。
6. **开机自启**：设置页开关。
7. **检查更新**：查询 npm 官方仓库最新 dsh 版本，与内置版本对比。
8. **日志**：设置页 → 关于与日志（完整日志在数据目录 `app.log`）。

## 三、填 API Key

首次进入界面会弹"添加一个 API Key"引导，填 DeepSeek 官方 key 即可（也可稍后配置）。key 存在 dsh 自己的数据目录（`data/dsh-home`），不在壳层。

## 四、维护指南

### 更新内置 dsh 版本（上游发新版后）
1. 改 `scripts/prepare-runtime.sh` 里 `DSH_VERSION=0.1.0-rc.6` 为新版本号；
2. 改 `main.js` 里 `DSH_VERSION = '0.1.0-rc.6'` 为同一版本号；
3. 跑 `bash scripts/prepare-runtime.sh`（重新拉依赖）；
4. 跑 `./node_modules/.bin/electron-builder --win` 重新打包；
5. 把新安装包发给用户（安装版覆盖安装，数据不受影响）。

### 开发模式调试
```bash
./node_modules/electron/dist/electron.exe .      # 开发模式（runtime 走 runtime-bundle/runtime）
DSH_DESKTOP_DEBUG=1 ...                           # 启动 8 秒后自动截图到数据目录 debug.png
```

### 已踩坑记录（换机器重装环境时必看）
- **npm 12 拦截 install scripts**：项目内安装必须把包名写进 package.json 的 `allowScripts` 字段，命令行 `--allow-scripts` 会被拒。
- **node-pty 原生模块**：npm 安装后不会自动链接，真实路径嵌套在 `node_modules/@deepseek-ai/dsh-subprocess-local/node_modules/node-pty`，需手动把 `prebuilds/win32-x64/*` 拷进其 `build/Release/`（prepare-runtime.sh 已自动处理）。
- **electron-builder 不拷 node_modules**：`extraResources` 拷贝源根目录下的 `node_modules` 会被硬编码过滤掉 → 必须套一层包装目录（`runtime-bundle/runtime/`），相对路径变成 `runtime/node_modules` 才能绕过。
- **Bun 单文件编译不可行**：dsh 依赖 Node 22 专有 API `node:module.stripTypeScriptTypes`，Bun 没有实现，启动即崩。
- **首次启动超时**：profile 初始化约 60~90 秒，健康检查超时给足 120 秒。

### 已知限制
- 未做代码签名：用户首次运行会看到 SmartScreen 提示（点"仍要运行"即可）；正式发布建议加签名证书（OV 约几百美元/年）。
- 便携版运行时会临时解包到 %TEMP%（electron-builder 机制），退出自动清理；数据不在此处，在 exe 旁 `data\`。

## 五、项目结构

```
DeepSeekHarness桌面版/
├── main.js               # 主进程（侧车管理/托盘/主题/角标/便携/快捷键/自启）
├── preload.js            # 安全 IPC 桥
├── settings.html         # 设置页（主题/角标/自启/便携/更新/日志）
├── loading.html          # 启动加载页
├── icon/icon.png         # 应用图标（scripts/make-icon.js 生成）
├── scripts/
│   ├── prepare-runtime.sh  # 固化运行时：node.exe + 固定版 dsh
│   └── make-icon.js        # 图标生成
├── runtime-bundle/runtime/ # 打包进安装包的运行时（node.exe + node_modules）
└── dist/                 # 产物：Setup 安装版 + 便携版
```
