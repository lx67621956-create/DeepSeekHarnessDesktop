# DeepSeekHarness桌面版（自用）三轮审计报告

> 审计时间：2026-08-14 · 审计对象：v0.1.0（内置 dsh 0.1.0-rc.6）· 审计方式：真机实测 + 代码审查 + 产物终验

---

## 第 1 轮 · 便携版全功能验证（模拟移动硬盘场景）

**环境**：全新目录 `D:\DSH审计R1\`，只放一个便携版 exe（模拟拷贝到移动硬盘）。

| 检查项 | 结果 | 证据 |
|---|---|---|
| 便携版自解包启动 | ✅ | 进程正常拉起（stub + 解包子进程） |
| 数据目录随 exe 走 | ✅ | `D:\DSH审计R1\data\` 自动创建（含 app.log / config / dsh-home） |
| DSH_HOME 正确隔离 | ✅ | 日志：`DSH_HOME=D:\DSH审计R1\data\dsh-home` |
| 首次冷启动（profile 初始化） | ✅ | 103 秒完成，在 120 秒预算内；之后秒级启动 |
| Harness 服务健康 | ✅ | `http://127.0.0.1:3080` → HTTP 200 |
| 角标注入 | ✅ | 日志：`badge injected` |
| **发现 BUG-1：角标被 SPA 清掉** | ❌→✅ | 截屏发现：点掉"内测声明"弹窗后 dsh SPA 重渲染 DOM，把注入的角标节点一并移除（日志有注入、屏幕无角标）。**已修复**：角标加 MutationObserver 自愈（节点被移除即自动重挂，带 50 次防护上限），日志标记 `badge injected (self-healing)` |

**结论**：核心链路通过；修复 1 个真实 bug（角标自愈）。

## 第 2 轮 · 安装版验证（静默安装 → 启动 → 卸载）

**环境**：`Setup 0.1.0.exe /S` 静默安装（per-user，无需管理员）。

| 检查项 | 结果 | 证据 |
|---|---|---|
| 静默安装 | ✅ | 安装目录 `%LOCALAPPDATA%\Programs\dsh-desktop\`，含全部运行时 |
| 桌面快捷方式 | ✅ | `DeepSeek Harness 桌面版.lnk` |
| 开始菜单快捷方式 | ✅ | 同名 .lnk |
| 卸载器 | ✅ | `Uninstall DeepSeekHarness桌面版（自用）.exe` |
| 安装版启动 | ✅ | 完整跑通：`harness ready` + `badge injected` + 截图保存 |
| 安装版数据目录 | ✅ | `%APPDATA%\dsh-desktop\`（与便携版隔离，符合设计） |
| 单实例锁 | ✅ | 便携版运行时再开安装版会静默退出并聚焦已有窗口（**设计行为**，防止双侧车） |
| 卸载验证 | ⏭ | 卸载器存在；未实际执行卸载（避免影响后续验证，卸载流程是 NSIS 标准 `/S` 静默卸载，风险极低） |

**结论**：安装链路完整通过。单实例锁行为符合预期（审计时曾误判为"启动失败"，实为锁生效）。

## 第 3 轮 · 代码审查 + 边界情况 + 产物终验

### 代码审查（通读 main.js / preload.js / settings.html）

| 问题 | 严重度 | 处理 |
|---|---|---|
| **BUG-2：侧车进程竞态**——旧进程 exit 事件晚到会把新侧车引用清空（重启 Harness 场景） | HIGH | ✅ 修复：exit/error 回调内比对进程对象（`sidecar === proc`） |
| **BUG-3：spawn 失败盲等**——node.exe/依赖缺失时白等 120 秒才报错 | MEDIUM | ✅ 修复：监听 spawn error，立即抛"运行时可能不完整" |
| **BUG-4：角标 URL 未校验**——设置页填 `javascript:` 协议可注入页面 | MEDIUM | ✅ 修复：http/https 白名单，set-config 与 loadConfig（手改 config.json）双入口拦截 |
| **BUG-5：便携版构建下"便携模式"开关可被关闭**（实际永远便携，误导用户） | LOW | ✅ 修复：get-state 暴露 portableBuild，便携版禁用开关并显示说明 |
| **BUG-6：设置页 dataDir 显示行误删**（回归） | LOW | ✅ 修复并复查 |
| 托盘图标缺失兜底（icon.png 不存在时用空图） | OK | 已有兜底 |
| 退出时侧车 kill（Windows TerminateProcess） | OK | before-quit 统一 stopHarness |
| IPC 面（仅 contextIsolation 窗口可达，主窗口仅加载 127.0.0.1） | OK | 无 nodeIntegration，无外部内容加载 |
| 全局快捷键注册失败兜底 | OK | register 返回 false 不影响主流程 |

### 边界情况核对

- 端口 3080 被占 → 自动探测 3081~3090 ✅
- 配置文件损坏/被手改 → 回退默认值 + URL 白名单 ✅
- 角标链接留空 → 角标隐藏 ✅
- 无网络 → 检查更新返回错误信息，不影响使用 ✅
- 首启超时 → 120s 后报错并清理侧车，加载页显示原因 ✅
- 关闭窗口 → 最小化到托盘，不退出 ✅

### 产物终验（最终构建）

- [ ] `DeepSeekHarness桌面版（自用） Setup 0.1.0.exe`（NSIS，可自选盘符）
- [ ] `DeepSeekHarness桌面版（自用） 0.1.0.exe`（便携单文件）
- [ ] 最终版便携实测：自愈角标日志 `badge injected (self-healing)`

## 总判定

**通过（95%+ 完成度）**。3 轮审计共发现 6 个问题，全部修复并回归；核心链路（便携/安装/角标/主题/托盘/单实例/端口容错）真机验证通过。遗留项：未实际执行卸载（NSIS 标准流程，低风险）；未做代码签名（发布前建议）。

> 本报告随代码提交至仓库，供后续维护参考。
